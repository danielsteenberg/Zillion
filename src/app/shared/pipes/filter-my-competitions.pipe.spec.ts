import { FilterMyCompetitionsPipe } from './filter-my-competitions.pipe';

describe('FilterMyCompetitionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMyCompetitionsPipe();
    expect(pipe).toBeTruthy();
  });
});
